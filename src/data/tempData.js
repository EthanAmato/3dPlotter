export const tempData = {
    x: Array.from({length: 40}, () => Math.floor(Math.random() * 40)),
    y: Array.from({length: 40}, () => Math.floor(Math.random() * 40)),
    z: Array.from({length: 40}, () => Math.floor(Math.random() * 40)),
}